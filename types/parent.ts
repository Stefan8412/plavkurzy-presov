export type Parent = {
  id: string;

  userId: string;

  firstName: string;
  lastName: string;

  phone: string;
  email: string;

  address?: string;

  createdAt: string;
  updatedAt: string;
};
